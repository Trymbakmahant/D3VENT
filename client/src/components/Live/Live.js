import Button from "../UI/Button";

const Live = () =>{

    return (
        <div>
            <Button classes = 'btn-error btn-wide'>⭕ Go Live</Button>
        </div>
    )
};

export default Live;